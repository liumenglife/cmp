# 问题九至十二签章智能体收尾修复记录

## 1. 修复范围

本次修复只覆盖质量审查报告第三章中的问题九、问题十、问题十一、问题十二，不修改 `docs/planning`，不提交 `git`，不回退既有改动。

## 2. 问题九：电子签章专项缺少可执行配置结构与算法基线

关闭状态：已关闭。

修改文件：

- `docs/technicals/modules/e-signature/special-designs/seal-resource-and-certificate-design.md`
- `docs/technicals/modules/e-signature/special-designs/signature-coordinate-and-rendering-design.md`
- `docs/technicals/modules/e-signature/special-designs/signing-party-orchestration-design.md`
- `docs/technicals/modules/e-signature/special-designs/signature-engine-adapter-design.md`
- `docs/technicals/modules/e-signature/special-designs/batch-resign-and-result-optimization-design.md`
- `docs/technicals/modules/e-signature/special-designs/signature-runtime-parameters-design.md`

修复内容：

- 六份电子签章专项均新增“最小可执行基线”章节。
- 每份专项均补齐配置结构、核心状态机、关键算法伪代码、输入输出样例、异常分流表、与详细设计字段的映射关系。
- `signature-engine-adapter-design.md` 额外补齐“内部参数到引擎参数映射表”和适配伪代码。
- 原“排除具体代码”口径已收敛为“不输出生产代码”，避免与新增伪代码和配置基线冲突。

验证方式：

- 编辑前后均重读六份电子签章专项，确认新增章节已落盘。
- 对照 `docs/technicals/modules/e-signature/detailed-design.md` 中 `es_signature_request`、`es_signature_session`、`es_signer_assignment`、`es_signature_result`、`es_signature_summary`、`es_request_document_binding`、`es_signature_audit_event` 等对象完成字段映射。

残余风险：

- 本次按专项设计层补齐可执行基线，未输出完整物理 `DDL`、正式 `OpenAPI schema` 或生产代码；这些仍应在实现阶段或正式接口定稿阶段承接。

## 3. 问题十：智能体底座委派回收后的运行态表述不一致

关闭状态：已关闭。

修改文件：

- `docs/technicals/foundations/agent-os/detailed-design.md`

修复内容：

- 保留 `P-A-O` 只是语义阶段描述、不作为 `ao_agent_run.runtime_state` 正式枚举的既有口径。
- 将父运行回收后的恢复目标从 `Perceive` 改为正式运行状态：回收成功进入 `INGRESS_PENDING` 或 `CONTEXT_GOVERNING`，由 `QueryEngine` 重新归一观察输入、上下文快照与下一步候选动作。
- 回收失败或结论冲突时进入 `TOOL_DECIDING`、`HUMAN_CONFIRMING` 或 `FAILED` 等正式运行状态。

验证方式：

- 编辑前重读 `10.3` 与 `10.5`，编辑后重读确认 `Perceive` 不再作为持久化恢复目标。

残余风险：

- 本次只修正文档运行态表述，未检查实现代码中是否存在同名状态枚举或迁移脚本。

## 4. 问题十一：专项设计总表缺少优先级与验收口径

关闭状态：已关闭。

修改文件：

- `docs/technicals/special-design-plan.md`

修复内容：

- 新增“专项设计总表”。
- 总表字段包含主线、专项文件、范围说明、上游依赖、下游影响、优先级、验收口径。
- 验收口径明确统一四项：边界已冻结；关键异常路径已覆盖；不与对应 `API Design` 和 `Detailed Design` 冲突；可被 `Implementation Batch Plan` 或对应主线 `Implementation Plan` 引用。
- 顺延后续章节编号，保证新增章节后文档编号连续。

验证方式：

- 编辑前后重读 `special-design-plan.md`，确认总表覆盖当前 10 条主线专项文件清单，并确认后续章节从第 6、7、8 节连续。

残余风险：

- 总表中的优先级用于实现准备和质量检查引用，不替代 `docs/technicals/implementation-batch-plan.md` 的正式批次顺序。

## 5. 问题十二：总平台接口设计章节编号跳号

关闭状态：已关闭。

修改文件：

- `docs/technicals/api-design.md`

修复内容：

- 确认第四节后不存在缺失正文，只是“接口分组”从第七节开始跳号。
- 将 `## 7. 接口分组` 改为 `## 5. 接口分组`。
- 将 `7.1` 至 `7.12` 顺延调整为 `5.1` 至 `5.12`。

验证方式：

- 编辑前后重读 `api-design.md` 相关章节，确认 `4.3` 后直接进入 `5. 接口分组`，子章节编号从 `5.1` 连续至 `5.12`。

残余风险：

- 本次只修正文档章节编号，未检查仓库其他文档是否引用旧的 `7.x` 章节号。

## 6. 汇总验证

已完成以下验证动作：

- 重读审查报告第三章问题九至问题十二，确认修复目标。
- 编辑前重读所有目标文件。
- 使用 `apply_patch` 完成所有文档修改。
- 编辑后重读所有已修改目标文件，确认内容落盘。
- 写入本修复记录。

未执行的验证动作：

- 本次为纯文档修复，未运行代码测试、类型检查或构建命令。
